{
  "name": "school-management-workspace",
  "version": "0.0.0",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "artifacts/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @workspace/school-portal",
    "build": "npm run build --workspace @workspace/school-portal",
    "typecheck": "npm run typecheck --workspace @workspace/school-portal"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
